CHIP Mux4Way {
    IN a, b, c, d, sel[2];
    OUT out;

    PARTS:
    // Put your code here:
    Mux(a = a, b = b, sel = sel[0], out = v1);
    Mux(a = c, b = d, sel = sel[0], out = v2);
    Mux(a = v1, b = v2, sel = sel[1], out = out);
}